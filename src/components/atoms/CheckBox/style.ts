import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  checkboxContainer: {
    flex: 1,
    padding: 20
  },
  checkbox: {
    height: 12,
    width: 12,
    borderRadius: 4,
    borderWidth: .5,
    borderColor: '#555555',
  }
})

export default styles;
